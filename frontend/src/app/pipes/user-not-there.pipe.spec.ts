import { UserNotTherePipe } from './user-not-there.pipe';

describe('UserNotTherePipe', () => {
  it('create an instance', () => {
    const pipe = new UserNotTherePipe();
    expect(pipe).toBeTruthy();
  });
});
