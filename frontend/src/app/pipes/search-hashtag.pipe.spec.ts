import { SearchHashtagPipe } from './search-hashtag.pipe';

describe('SearchHashtagPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHashtagPipe();
    expect(pipe).toBeTruthy();
  });
});
